export enum Routes {
    AUTH = 'auth',
    SIGN_IN = 'signin',
    SIGN_UP = 'signup'
}

export enum Services {
    GATEWAY_SESSION_MANAGER = 'GATEWAY_SESSION_MANAGER'
}

export enum WebsocketEvents {
   
}
